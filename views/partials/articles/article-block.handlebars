<div class="card p-2">
  <div class="card-header p-0">
    <ul class="nav nav-pills card-header-pills">
      <li class="nav-item">
        <a class="nav-link" href="{{link}}">View</a>
      </li>
        <li class="nav-item">
        {{#if scrape}}
          <a class="nav-link" data-link="{{link}}" data-title="{{title}}" id="saveArticle" href="#" ></i>Save</a>
        {{else}}
          <a class="nav-link" data-id="{{_id}}" id="deleteArticle" href="#" >Delete</a>
        {{/if}}
        </li>
    </ul>
  </div>
  <div class="card-body">
    <p> {{title}} </p>
  </div>
</div>